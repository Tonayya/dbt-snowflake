version: 2

models:
  - name: abc
    description: bla bla
  - name: foo
    access: public
    description: testing stuff

  - name: incremental
    config:
      materialized: incremental
      incremental_strategy: merge
