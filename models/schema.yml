version: 2

models:
  - name: model_a
    config:
      tags: ['some_tag'] 
    columns:
      - name: id
        description: bla
        tests:
          - not_null

  - name: from_model_a
    columns:
      - name: id
        tests:
          - not_null
